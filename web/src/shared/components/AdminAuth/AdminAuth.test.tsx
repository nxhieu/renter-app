import * as React from 'react';
import { shallow } from 'enzyme';

import AdminAuth from './AdminAuth';

describe('AdminAuth', () => {
    const defaultProps = {};

    it('tests something', () => {
        shallow(<AdminAuth {...defaultProps} />);
    });
});
