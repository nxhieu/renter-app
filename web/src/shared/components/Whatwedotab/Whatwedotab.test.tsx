import * as React from 'react';
import { shallow } from 'enzyme';

import Whatwedotab from './Whatwedotab';

describe('Whatwedotab', () => {
    const defaultProps = {};

    it('tests something', () => {
        // shallow(<Whatwedotab {...defaultProps} />);
    });
});
