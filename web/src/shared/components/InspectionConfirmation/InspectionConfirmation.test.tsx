import * as React from 'react';
import { shallow } from 'enzyme';

import InspectionConfirmation from './InspectionConfirmation';

describe('InspectionConfirmation', () => {
    const defaultProps = {};

    it('tests something', () => {
        shallow(<InspectionConfirmation {...defaultProps} />);
    });
});
