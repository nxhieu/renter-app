import * as React from 'react';
import { shallow } from 'enzyme';

import Routingbutton from './Routingbutton';

describe('Routingbutton', () => {
    const defaultProps = {};

    it('tests something', () => {
        shallow(<Routingbutton {...defaultProps} />);
    });
});
