import * as React from 'react';
import { shallow } from 'enzyme';

import Roomcarousel from './Roomcarousel';

describe('Roomcarousel', () => {
    const defaultProps = {};

    it('tests something', () => {
        shallow(<Roomcarousel {...defaultProps} />);
    });
});
