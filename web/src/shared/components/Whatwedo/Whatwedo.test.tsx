import * as React from 'react';
import { shallow } from 'enzyme';

import Whatwedo from './Whatwedo';

describe('Whatweddo', () => {
    const defaultProps = {};

    it('tests something', () => {
        // shallow(<Whatweddo {...defaultProps} />);
    });
});
